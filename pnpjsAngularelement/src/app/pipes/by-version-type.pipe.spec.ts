import { ByVersionTypePipe } from './by-version-type.pipe';

describe('ByVersionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ByVersionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
